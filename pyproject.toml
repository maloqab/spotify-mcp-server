[project]
name = "spotify-mcp-server"
version = "0.1.0"
description = "Spotify MCP server for Claude Code"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.20.0",
    "spotipy>=2.24.0",
]

[project.scripts]
spotify-mcp-server = "server:main"
